# LLM-Fraud-Detection
Python script that utilizes OpenAI API and GPT-4o-mini to analyze contents of an email and returns a JSON structured output containing a scam score and an explanation as to why the email is fraudulent or not.
